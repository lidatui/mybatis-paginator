<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="financial.dict">

	<select id="find" resultType="map">
        <if test="sortInfos != null and sortInfos.size() != 0 ">
            select * from (
        </if>

		select * from FP_BASE_DICT where type like #{type} || '%'

        <if test="sortInfos != null and sortInfos.size() != 0">
            ) order by
            <foreach item="sortInfo" index="index" collection="sortInfos" open="" separator="," close="">
                ${sortInfo.columnName} ${sortInfo.sortStatus}
            </foreach>
        </if>
	</select>

</mapper>